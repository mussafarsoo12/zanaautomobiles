[build]
  command = "npm run build"
  publish = "dist/public"

# Optional: if you want to proxy `/api/*` to a backend, uncomment and set `to`.
# [[redirects]]
#   from = "/api/*"
#   to = "https://api.example.com/:splat"
#   status = 200
#   force = true
